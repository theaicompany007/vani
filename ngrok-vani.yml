# Ngrok Configuration for VANI Outreach Command Center
# Location: /home/postgres/vani/ngrok-vani.yml
#
# This config creates a stable production endpoint for VANI
# Domain: vani.ngrok.app (reserved domain)
# Port: 5000 (VANI Flask app)
#
# Usage:
#   ngrok start --config ngrok-vani.yml vani
#
# Auto-start: Configured via systemd service (ngrok-vani.service)
#
# Notes:
#   - This is one of 2 production endpoints (revenue + vani)
#   - Third endpoint reserved for ad-hoc development
#   - Auto-reconnects on VM reboot

version: "2"
authtoken: 35hWIb0qQ1hFTb6coNwwVN2lwnw_2ANeLdD3x6mjkH2yRB7f1  # From ngrok.config.json (theaicompany007 account)

tunnels:
  vani:
    proto: http
    addr: 5000
    domain: vani.ngrok.app
    inspect: false  # Disable web inspector for production
    bind_tls: true  # Force HTTPS

